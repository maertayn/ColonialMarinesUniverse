<controls:DepartmentConsoleWindow
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.CMU14.ColonyEconomy"
    Title="{Loc department-console-title}" MinSize="600 520">
    <TabContainer>
        <!-- Management Tab -->
        <BoxContainer Name="ManagementTab" Orientation="Vertical" SeparationOverride="4">
            <!-- Header -->
            <Label Name="DeptNameLabel" Access="Public" Text="{Loc department-console-department}" StyleClasses="LabelHeading" />
            <Label Name="DeptBudgetLabel" Access="Public" />

            <!-- Withdraw from Department Budget -->
            <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                <Label Text="{Loc department-console-withdraw-label}" />
                <LineEdit Name="WithdrawAmountInput" Access="Public" PlaceHolder="{Loc department-console-amount-placeholder}"
                          HorizontalExpand="True" SizeFlagsStretchRatio="2" />
                <Button Name="WithdrawButton" Access="Public" Text="{Loc department-console-withdraw-cash}" />
            </BoxContainer>

            <!-- Default Salary -->
            <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                <Label Text="{Loc department-console-default-salary-label}" />
                <LineEdit Name="DefaultSalaryInput" Access="Public" Text="50"
                          HorizontalExpand="True" SizeFlagsStretchRatio="2" />
                <Button Name="SetDefaultSalaryButton" Access="Public" Text="{Loc department-console-set-default-salary}" />
            </BoxContainer>

            <!-- Hire -->
            <Label Text="{Loc department-console-hire-hint}" StyleClasses="LabelSubText"
                   Margin="0 2" />

            <!-- Employee List -->
            <Label Text="{Loc department-console-employees}" StyleClasses="LabelSubText" />
            <ScrollContainer VerticalExpand="True" HScrollEnabled="False" MinSize="0 180">
                <BoxContainer Name="EmployeeList" Access="Public" Orientation="Vertical" SeparationOverride="2" />
            </ScrollContainer>

            <!-- Department Announcement -->
            <Label Text="{Loc department-console-announcement}" StyleClasses="LabelSubText" />
            <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                <LineEdit Name="AnnouncementInput" Access="Public" PlaceHolder="{Loc department-console-announcement-placeholder}"
                          HorizontalExpand="True" SizeFlagsStretchRatio="3" />
                <Button Name="SendAnnouncementButton" Access="Public" Text="{Loc department-console-send}" />
            </BoxContainer>
        </BoxContainer>

        <!-- Orders Tab -->
        <BoxContainer Name="OrdersTab" Orientation="Vertical" SeparationOverride="4">
            <Label Name="OrdersBudgetLabel" Access="Public" StyleClasses="LabelHeading" />

            <!-- Reason / Deliver To fields -->
            <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                <Label Text="{Loc department-console-reason}" MinWidth="70" />
                <LineEdit Name="OrderReasonInput" Access="Public" PlaceHolder="{Loc department-console-reason-placeholder}"
                          HorizontalExpand="True" />
            </BoxContainer>
            <BoxContainer Orientation="Horizontal" SeparationOverride="4">
                <Label Text="{Loc department-console-deliver-to}" MinWidth="70" />
                <LineEdit Name="OrderDeliverToInput" Access="Public" PlaceHolder="{Loc department-console-delivery-placeholder}"
                          HorizontalExpand="True" />
            </BoxContainer>

            <!-- Category buttons -->
            <Label Text="{Loc department-console-categories}" StyleClasses="LabelSubText" />
            <ScrollContainer HScrollEnabled="True" VScrollEnabled="False" MinSize="0 36">
                <BoxContainer Name="OrderCategoryList" Access="Public" Orientation="Horizontal" SeparationOverride="4" />
            </ScrollContainer>

            <!-- Item list for selected category -->
            <Label Name="SelectedCategoryLabel" Access="Public" Text="{Loc department-console-select-category}" StyleClasses="LabelSubText" />
            <ScrollContainer VerticalExpand="True" HScrollEnabled="False" MinSize="0 200">
                <BoxContainer Name="OrderItemList" Access="Public" Orientation="Vertical" SeparationOverride="2" />
            </ScrollContainer>
        </BoxContainer>
    </TabContainer>
</controls:DepartmentConsoleWindow>
